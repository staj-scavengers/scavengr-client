<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:padding="4dp"
  tools:context=".CurrentClueFragment">

  <TextureView
    android:id="@+id/camera_frame"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    />

<!--  Text is place holder for input from database-->
  <TextView
    android:id="@+id/clue_number"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Clue Number #"
    app:layout_constraintBottom_toTopOf="@id/clue_name"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

  <!--  Text is place holder for input from database-->
  <TextView
    android:id="@+id/clue_name"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="CLUE NAME"
    android:gravity="center"
    android:textSize="10pt"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

  <Button
    android:id="@+id/go_to_scanner_button"
    android:layout_width="150dp"
    android:layout_height="wrap_content"
    android:text="SCAN"
    app:layout_constraintTop_toBottomOf="@id/clue_name"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toStartOf="@id/skip_clue_button"/>

    <Button
      android:id="@+id/skip_clue_button"
      android:layout_width="150dp"
      android:layout_height="wrap_content"
      android:text="SKIP"
      app:layout_constraintTop_toBottomOf="@id/clue_name"
      app:layout_constraintStart_toEndOf="@id/go_to_scanner_button"
      app:layout_constraintEnd_toEndOf="parent"/>

  <TextView
    android:id="@+id/clue_description"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toBottomOf="@id/go_to_scanner_button"/>

</androidx.constraintlayout.widget.ConstraintLayout>